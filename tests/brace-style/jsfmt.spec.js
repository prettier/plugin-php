run_spec(import.meta, ["php"]);
run_spec(import.meta, ["php"], { braceStyle: "psr-2" });
run_spec(import.meta, ["php"], { braceStyle: "per-cs" });
run_spec(import.meta, ["php"], { braceStyle: "1tbs" });
